aiofiles==24.1.0               # Fayl yuklashda, faylni async bilan o'qish/yozish
aiogram==3.20.0.post0          # Telegram bot framework
aiohttp==3.11.18               # HTTP so'rovlar (Telegram bilan bog'lanish)
asyncpg==0.30.0                # PostgreSQL bilan async ishlash
python-dotenv==1.1.0           # .env fayldan konfiguratsiya o'qish
openpyxl==3.1.5                # Excel fayllar bilan ishlash (agar yuklash/import qilish bo'lsa)
aiofiles==24.1.0               # Fayl yuklashda, faylni async bilan o'qish/yozish
aiogram==3.20.0.post0          # Telegram bot framework
aiohttp==3.11.18               # HTTP so'rovlar (Telegram bilan bog'lanish)
asyncpg==0.30.0                # PostgreSQL bilan async ishlash
python-dotenv==1.1.0           # .env fayldan konfiguratsiya o'qish
openpyxl==3.1.5                # Excel fayllar bilan ishlash (agar yuklash/import qilish bo'lsa)
